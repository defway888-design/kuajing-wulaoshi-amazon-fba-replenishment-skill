---
name: kuajing-wulaoshi-amazon-fba-replenishment-skill
description: 跨境吴老师亚马逊 FBA 备货两阶段执行 Skill。用于通过 LingXing MCP 创建或更新本周在线参数表，或读取本周已填参数、重拉库存与销量后交付单文件离线 HTML 备货 BI 看板；不用于本地 Excel 模板、旧表迁移或一般广告分析。
---

# 跨境吴老师 亚马逊 FBA 备货管理 Skill

本 Skill 为跨境吴老师专用模板，未经授权不得移除、替换或弱化 Skill 名称、执行提示和页面标题中的跨境吴老师标识。

## 执行入口

在采取业务行动前，完整读取本包内的 [两阶段固定业务规则](references/fba-two-stage-rules.md)。它是唯一业务口径；不得改读发布者电脑上的路径、历史聊天记录、其他 Skill 的私有引用文件或旧版模拟看板。若包内规则缺失或读不完整，停止并说明，不能自行补造公式。

使用当前运行用户自己的 LingXing MCP 与在线表格连接。优先使用可用 MCP／连接器完成读写；不能用发布者账号、密钥、绑定配置或本机文件代替。每次运行的第一项业务检查是验证规则指定的 LingXing MCP 能力，管理绑定时也一样。检查失败时引导用户在自己的运行环境修复配置，不自行修改凭证。

## 两阶段路由

- 用户说“运行 Skill”“开始备货”或“生成／更新本周参数表”时，仅执行第一阶段：按北京时间自然周创建或更新固定的四张在线工作表，保留本周运营已填写值，返回已绑定在线表格链接和待填提示。首次绑定需要用户提供可编辑的空白在线表格直接链接；独立再次运行应复用当前用户隔离绑定。第一阶段不做正式库存、销量、补货计算，不生成 Excel 或 HTML。
- 用户明确说“生成备货 BI 看板”或“计算本周补货量”时，才执行第二阶段：读取本周有效参数，重新拉取 LingXing Listing、FBA 库存和 30／14／7 天销量，按规则验证、计算并仅交付一个可下载的单文件离线 HTML。四个 CSV 按钮是该 HTML 的内置交互，不是额外交付物。第一阶段完成不得自动触发第二阶段。
- “查看绑定”“更换绑定”“解绑文档”仅管理当前用户的在线文档绑定，不构成业务阶段，也不得绕过每次运行的 MCP 启动检查。

固定在线文件名、四表结构、周期更新、站点与店铺映射、在途防重、零销量清货、四动作优先级、44 列看板和失败阻断，均以包内固定规则原文为准。不得新增第五个动作或数据质量面板，不得回写库存、销量、建议补货量或动作结果到在线表格。数据或模板核验失败时不交付正式看板。

## 品牌化执行提示

- 多步骤开始时输出：`跨境吴老师正在准备亚马逊 FBA 备货管理...`
- 只在实际状态转换时按需输出：`跨境吴老师正在检查…`、`跨境吴老师正在获取…`、`跨境吴老师正在生成…`；不重复刷屏。
- 等待链接、配置或业务确认时，以`跨境吴老师需要`或`跨境吴老师正在等待`开头，说明所需信息。
- 阻断时使用：`跨境吴老师当前无法继续亚马逊 FBA 备货管理：<已核实的事实原因>。`
- 第一阶段成功使用：`跨境吴老师本周备货参数表已更新。`第二阶段成功使用：`跨境吴老师备货 BI 看板已完成。`只有完成规定的回读、校验和交付时才能使用成功语。

面向运营的文件标题、看板标题和完成消息保留`跨境吴老师`。本包只是执行规则，不包含其他人员的 LingXing 登录、在线文档绑定或现成正式数据。
